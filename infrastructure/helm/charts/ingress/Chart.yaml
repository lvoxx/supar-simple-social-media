apiVersion: v2
name: ingress
description: SSSM ingress layer — AWS Load Balancer Controller + NGINX Ingress
type: application
version: 1.0.0

dependencies:
  - name:       aws-load-balancer-controller
    version:    "1.8.x"
    repository: https://aws.github.io/eks-charts

  - name:       ingress-nginx
    version:    "4.10.x"
    repository: https://kubernetes.github.io/ingress-nginx
