apiVersion: v1
kind: Service
metadata:
  name: media-guard-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: media-guard-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    app: media-guard-service
